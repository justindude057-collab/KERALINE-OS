[org 0x7C00]        
bits 16

start:
    mov si, message

print_loop:
    lodsb             
    cmp al, 0
    je done

    mov ah, 0x0E       
    int 0x10

    jmp print_loop

done:
    jmp $               

message db "OS RECOVERY MODE", 0

times 510 - ($ - $$) db 0
dw 0xAA55              
